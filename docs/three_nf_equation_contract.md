# Three-Nucleon-Force Equation Contract for Tic-tac

**Status:** authoritative reference. Every C++ object in the 3NF / Faddeev path
must be traceable to a symbol defined here. **No empirical scaling factor,
post-hoc Hermitian averaging, or hand-flipped sign is permitted to make a curve
agree with a reference.** When a measurement disagrees with this contract, the
*derivation* in this document is the court of appeal, not the reference curve.

This document is Phase 0 of the fix branch `fix/3nf-physics-contract`. It is the
prerequisite for every later phase: the finite-dimensional operator oracle
(Phase 0 acceptance), the c₄ / c_E fixes (Phases 1–2), the independent angular
oracle (Phase 3), and the kernel unification (Phase 5) all read their *expected*
operator algebra from §6 below.

---

## 1. Conventions and notation

| Symbol | Meaning |
|--------|---------|
| `p`, `q` | Jacobi momenta in the spectator-1 frame: `p` = pair-relative (particles 2,3), `q` = spectator (particle 1). Units: fm⁻¹. |
| `G0 = (E − H0 + iε)⁻¹` | free 3-body resolvent |
| `t` | fully-dressed two-body t-matrix in the (2,3) pair subspace (a function of `p,p'` at fixed spectator `q`) |
| `P = P₁₂₃ + P₁₃₂` | sum of cyclic particle permutations. For fully **antisymmetric** pair states `P₁₂₃ = P₁₃₂`, so `P = 2·P₁₂₃`. |
| `V^(1)` | the spectator-1 component of the 3NF: particle 1 is spectator, the operator acts symmetrically under 2↔3. The full physical 3NF is `V₃ = (1+P+P²)·V^(1)`. |
| `W^(1)` | the spectator-1 3NF operator **as it appears in the AGS kernel**. See §5 for the exact relation to `V^(1)`. |
| `φ`, `Φ` | asymptotic channel state (deuteron + spectator), antisymmetric |
| `C` | SWP→WP basis-rotation matrix (the `C_WP` arrays) |
| `G` | **channel resolvent**: the SWP-diagonal resolvent obtained after rotating through `C`. `G ≠ G0`. |

Literature lock (unchanged from `docs/3nf_audit_2026-06-21.md`):

- E2002 — Epelbaum et al., PRC **66** (2002) 064001. Operator forms, LECs,
  regulator eq. (3.19), contact PWD App. A.
- G2010 — Golak et al., EPJ A **43** (2010) 241. General 5D PWD algorithm.
- H2015 — Hebeler et al., PRC **91** (2015) 044001. LS-coupled factorised PWD.
- W2008 — Witała et al., PRC **77** (2008) 034004. AGS equation with 3NF,
  operator ordering eq. (3).

---

## 2. The continuous Faddeev equation with 3NF

The starting point (given verbatim in the task specification; matches W2008
eq. (2) and E2002 §2) is the spectator-1 Faddeev component `T` of the full
3-body scattering operator, driven by both the 2-body t-matrix and the 3NF:

```
T|φ⟩ = t·P|φ⟩
      + (1 + t·G0)·V^(1)·(1 + P)|φ⟩              … (i) 3NF Born
      + t·P·G0·T|φ⟩                              … (ii) 2NF kernel
      + (1 + t·G0)·V^(1)·(1 + P)·G0·T|φ⟩        … (iii) 3NF kernel
```

The four pieces have distinct roles:

1. `t·P|φ⟩` — 2NF Born (one application of the dressed pair t, with spectator
   permutation `P`).
2. `(1 + t·G0)·V^(1)·(1 + P)|φ⟩` — **3NF Born**, direct. The `(1+P)` antisymmetrises
   the *ket*; the `(1+t·G0)` dresses the 3NF by the pair t-matrix.
3. `t·P·G0·T|φ⟩` — 2NF rescattering kernel.
4. `(1 + t·G0)·V^(1)·(1 + P)·G0·T|φ⟩` — **3NF rescattering kernel**. Same dressing
   `(1+t·G0)` as (ii); the extra `G0·T` is one more rescattering.

The unknown operator is `T` — the spectator-1 Faddeev component of the full
3-body scattering amplitude. The full physical amplitude between antisymmetric
states is `T_full = (1 + P + P²)·T` evaluated between antisymmetric `⟨Φ|, |Φ⟩`.

---

## 3. Reduction to the AGS form

Use `t = V + V·G0·t` (Lippmann–Schwinger) so that

```
(1 + t·G0)·V^(1) = V^(1) + t·G0·V^(1)      … (*)
```

i.e. the `(1+t·G0)` dressing of `V^(1)` is `V^(1)` (bare) plus the pair-t
rescattering `t·G0·V^(1)`. Substituting (*) into the Faddeev equation and
collecting the **undressed** 3NF `V^(1)·(1+P)` as the *direct* part of the kernel
while the `t·G0·V^(1)` part is generated by the *iteration* (the same `t·P·G0`
machinery that dresses the 2NF), the Faddeev equation reorganises into the AGS
form (W2008 eq. (3)):

```
U = K + K·G·U ,        with the AGS kernel
K = P·V + W^(1)·(1 + P) ,
```

where:

- `U` is the AGS scattering operator (the code's `U_array`). The relation to the
  Faddeev `T` is `U = G0⁻¹·T·G0⁻¹`; on-shell matrix elements of `U` between
  deuteron-spectator channel states are the physical elastic / breakup
  amplitudes.
- `G` is the **channel resolvent** `G = (E − H0 − V_pair)⁻¹` in the
  single-channel SWP basis. It is **not** the free `G0`; it already carries the
  pair interaction `V_pair`, so the pair t-matrix `t` is *implicit* in `G`.
- `W^(1)` is the spectator-1 3NF operator **as it enters the AGS kernel**.

### 3.1 What `W^(1)` is, precisely

Comparing the Faddeev and AGS forms term-by-term:

| Faddeev piece | AGS contribution | Dressing source |
|---------------|------------------|------------------|
| `t·P\|φ⟩` (2NF Born) | `P·V` in `K` | the `t = V + V·G0·t` iteration is produced by `K·G·U` (because `G` carries `V_pair`) |
| `(1+t·G0)·V^(1)·(1+P)\|φ⟩` (3NF Born) | `V^(1)·(1+P)` in `K` | the bare `V^(1)` sits in `K`; the `t·G0·V^(1)` dressing comes from `P·V·G·V^(1)·(1+P)` in `K·G·K` |
| `t·P·G0·T` (2NF kernel) | `K·G·U` rescattering | generated by iteration |
| `(1+t·G0)·V^(1)·(1+P)·G0·T` (3NF kernel) | `V^(1)·(1+P)·G·U` rescattering | generated by iteration |

**Conclusion:** in the AGS/WPCD form, `W^(1)` *is the bare spectator-1 3NF
`V^(1)`*. The `(1+t·G0)` dressing is **not** put in by hand; it is produced by
the `K·G·U` iteration, exactly as the 2NF dressing `t = V + V·G0·t` is produced
by `P·V·G·P·V` iteration. This is why the code's `W1_element` evaluates the bare
`c_E, c_D, c_1, c_3` matrix elements and never adds a `(1+t·G0)` factor.

### 3.2 Why the 2NF driving term is `P·V` (and not `P·t`)

In the AGS equation the 2NF enters the kernel as `P·V` — the *potential*, not
the t-matrix. The reason is the same as §3.1: the t-matrix `t = V + V·G0·t` is
generated by the `K·G·U` iteration, where `G` already contains `V_pair`. If one
drove the kernel with `P·t` instead, the pair interaction would be
double-counted (once inside `t`, once inside `G`). Hence `P·V`.

### 3.3 Why the 3NF driving term is `V^(1)·(1+P)` and not `(1+P)·V^(1)`

`V^(1)` is *defined* in the spectator-1 frame. The cyclic permutations `P₁₂₃,
P₁₃₂` move particle 1 into the pair and another particle into the spectator
slot, producing `V^(2), V^(3)`. In the AGS kernel acting in spectator-1 Faddeev
space, the only well-defined 3NF object is `V^(1)`; left-multiplying by `P`
would move `V^(1)` out of its definition frame. The ket-side `(1+P)`, by
contrast, simply sums the spectator-1, spectator-2, spectator-3 components of
the *channel state* — this is well-defined.

Therefore the kernel uses `V^(1)·(1+P)`: `V^(1)` on the **left** (in its own
frame), `(1+P)` on the **right** (acting on the channel ket). This is W2008
eq. (3) and G2010 §2 verbatim. The equality `V^(1)·(1+P) = (1+P)·V^(1)` holds
**only** as a matrix element between fully antisymmetric states; in the
*kernel* — where the ket is a single Faddeev component, not an antisymmetrised
3N state — the two forms are physically different operators. The dense test
`tests/cpp/test_faddeev_operator_order.cpp` locks this with deliberately
non-symmetric mock `W^(1), V, P, C` so a regression cannot hide behind
symmetry.

### 3.4 Is `(1+t·G0)` fully absorbed into `G`?

**Yes — into the channel resolvent `G`, not into `W^(1)`.** The channel
resolvent `G = (E − H0 − V_pair)⁻¹` diagonalised in the SWP basis already
contains the pair interaction `V_pair`. The pair t-matrix is the on-shell
amplitude derived from `V_pair` via `t = V + V·G0·t`; because `G` carries
`V_pair`, the `K·G·U` iteration generates the dressing. There is no leftover
`(1+t·G0)` factor to insert into `W^(1)` by hand — doing so would double-dress
the 3NF.

### 3.5 Elastic amplitude: does the direct `V^(1)·(1+P)` term appear once?

**Yes, exactly once — in the Born (n=0) Neumann coefficient.** The WPCD
iteration (§4) builds `U = K + K·G·K + K·G·K·G·K + …` Padé-resummed. The first
term `K` already contains `V^(1)·(1+P)`; every later `K` in `K·G·K·G·…` also
contains `V^(1)·(1+P)`, but those are rescattering corrections, not "another
direct term". The code's `n=0` Neumann term `a_0 = A = C^T·K·C` (see §4) is the
one place the direct 3NF Born appears at leading order, and it appears once.

### 3.6 Breakup amplitude: does `(1+P)·T` include the 3NF?

The breakup amplitude is `⟨Φ_3| (1+P)·T |Φ⟩` where `|Φ_3⟩` is a 3-free-particle
asymptotic state and `T` is the Faddeev component. In AGS form this becomes
`⟨Φ_3| (1+P)·G0·U·G0 |Φ⟩`. Because `U` is built from the kernel `K` containing
`V^(1)·(1+P)`, the 3NF contribution to breakup enters (i) through every `K` in
the `U = K + KGU + …` series and (ii) through the outer `(1+P)`. The code's
breakup path (`U_BU_array`) inherits this structure automatically because the
same `U` is used; no separate 3NF insertion is needed for breakup. (Tic-tac's
production breakup flag is currently `false`; the contract still applies.)

---

## 4. WPCD / SWP discretisation of the AGS equation

### 4.1 Bases

Tic-tac uses three nested bases for the (α, p, q) discretisation:

1. **WP basis** (wave packets): momentum-space bins `[p_i, p_i+Δp_i]` and
   `[q_j, q_j+Δq_j]` with midpoint quadrature. State label
   `|α, i, j⟩_WP`. Dimension `Nα·Np_WP·Nq_WP` (= `dense_dim` in the code).
2. **SWP basis** (shifted wave packets): the WP basis diagonalising the
   pair Hamiltonian `H_pair = H0_pair + V_pair`. The transformation is `C`
   (`C_WP_unco_array`, `C_WP_coup_array`). SWP eigenvalues are the
   `e_SWP_*_array` used by `make_resolvent`.
3. **Channel basis**: the (α) partial-wave label `α = (L₂N, S₂N, J₂N, T₂N,
   L₁N, 2j₁N; 2J₃N, 2T₃N, P₃N)`. Conservation of `2J₃N, 2T₃N, P₃N` block-diagonalises
   every operator.

### 4.2 The WP-basis AGS kernel

Rotate the AGS kernel `K = P·V + W^(1)·(1+P)` into the WP basis and apply the
SWP rotation `C` on the pair line:

```
A_α'α  ≡  (C^T · K · C)_{α'α}
        =  (C^T · P · V · C)_{α'α}                  … 2NF driving  (PV term)
         +  (C^T · W^(1) · C)_{α'α}                 … 3NF identity  (W1·C term)
         +  (C^T · W^(1) · P · C)_{α'α}              … 3NF permutation (W1·P·C term)
```

This is exactly the three contributions the code assembles inside
`calculate_CPVC_col` / `calculate_all_CPVC_rows`:

| Math object | C++ array / function |
|-------------|----------------------|
| `C` (pair SWP rotation) | `C_WP_unco_array`, `C_WP_coup_array`; viewed row-major as `CT_RM_array[α·Nα+α']` |
| `V` (2NF pair potential) | `V_WP_unco_array`, `V_WP_coup_array`; combined with `C` into `VC_CM_array[α·Nα+α']` |
| `P = P₁₂₃ + P₁₃₂` (cyclic permutation, sparse) | `P123_sparse_{val,row,col}_array` |
| `W^(1)` (bare spectator-1 3NF matrix element) | `three_nucleon_force_model::W1_element(α_r, α_c, p_r, q_r, p_c, q_c, pw_states)` |
| `PVC = P·V·C` column | `calculate_PVC_col` → `PVC_col[]` |
| `W^(1)·C` column | inline in `calculate_CPVC_col` (sum over `idx_p_j` with `C_block[idx_p_c·Np+idx_p_j]`) |
| `W^(1)·P·C` column | `calculate_PVC_col` on `CT_RM_array` (= `P·C`) then `W^(1)` applied in the `idx_alpha_k` loop |
| `A = C^T·(PVC + W1·C + W1·P·C)` column | `calculate_CPVC_col` → `col_array[]`; row form `calculate_all_CPVC_rows` → `row_arrays[]` |

The two code paths (`calculate_CPVC_col` and `calculate_all_CPVC_rows`) compute
**the same operator `A`** — one column-at-a-time (for the sparse precalculated
kernel and the dense solver) and one row-at-a-time (for the on-shell driving
term and the Neumann iteration). Phase 5 of this fix unifies them behind a
single `apply_W1_one_plus_P_to_column` helper; until then, both must agree to
machine precision (operator-level oracle, §6).

### 4.3 The Neumann / Padé iteration

Once `A = C^T·K·C` is formed, the AGS equation `U = K + K·G·U` becomes the
matrix iteration

```
U_WP = A + A·G_WP·U_WP ,
```

with `G_WP` the SWP-diagonal channel resolvent (`G_array` in the code, built by
`make_resolvent` from `e_SWP_*_array`). Tic-tac expands this as the Neumann
series

```
a_0 = A ,   a_{n+1} = a_n · G_WP · A ,    U_WP ≈ Σ_{n=0}^{N} a_n ,
```

then Padé-resums `[N/N]` of the `a_n` to recover the on-shell elastic
`U_array`. The 3NF enters **only** through `A` (i.e. through `K`); the
resolvent `G_WP` is *independent* of the 3NF LECs `c_D, c_E, c_1, c_3, c_4`
(the channel resolvent is the *2-body* pair resolvent, not a 3-body object).

### 4.4 Why `W^(1)·P·C` reuses `calculate_PVC_col` on `CT_RM_array`

`P·C` means "apply the cyclic permutation `P = P₁₂₃+P₁₃₂` to a column of `C`".
The existing `calculate_PVC_col` routine computes `P·V·C` for arbitrary
right-factor `V·C` supplied via `VC_CM_array`. Substituting `V → 1` (i.e.
`VC_CM_array = CT_RM_array`, the identity in the pair line) makes
`calculate_PVC_col` compute `P·1·C = P·C`. The code reuses this for the
`W^(1)·P·C` piece, which is algebraically correct because `P` is the same
operator in both cases — only the pair-line factor (`V` vs `1`) differs.

---

## 5. Operator ordering: the four questions answered

| Question (from task §Phase 0) | Answer |
|------------------------------|--------|
| What is the unknown operator? | The AGS `U` (`U_array`). Equivalent to the Faddeev `T` via `U = G0⁻¹·T·G0⁻¹`. On-shell `U`-matrix elements between deuteron-spectator channel states are the physical elastic/breakup amplitudes. |
| Why is the 2NF driving `P·V` (not `P·t`)? | Because `G` already carries `V_pair`, so the `t = V + V·G0·t` dressing is generated by `K·G·U`. Driving with `P·t` would double-count the pair interaction. |
| What is the 3NF driving, `V^(1)·(1+P)` or `(1+P)·V^(1)`? | `V^(1)·(1+P)`, with `V^(1)` on the LEFT in its own spectator-1 frame. `(1+P)` acts on the channel ket. The reverse order holds only between fully antisymmetric states, not in the kernel. |
| Is `(1+t·G0)` fully absorbed? | Yes — into the channel resolvent `G` (which carries `V_pair`), NOT into `W^(1)`. The code's `W1_element` correctly evaluates the bare `V^(1)` and adds no `(1+t·G0)` factor; the dressing is produced by `K·G·U` iteration. |
| Does the direct `V^(1)·(1+P)` term appear once in the elastic amplitude? | Yes, exactly once — as the `n=0` Born term `a_0 = A = C^T·K·C`. Every later occurrence is a rescattering correction, not another direct term. |
| Does breakup `(1+P)·T` include the 3NF? | Yes, via `U` (which carries `V^(1)·(1+P)` in `K`) and the outer `(1+P)`. No separate 3NF insertion is required. |

---

## 6. Finite-dimensional operator-level oracle (Phase 0 acceptance)

Before any physics tuning, the algebra of §4.2 must hold on a small
artificial matrix. The oracle lives in
`tests/cpp/test_3nf_operator_oracle.cpp` and is deliberately independent of
the production 3NF recoupling / kernels: it builds *random* but
symmetry-respecting `P, V, W1, G1, C` and checks that **every code path** that
claims to compute `A = C^T·(P·V + W^(1)·(1+P))·C` agrees with the mathematical
expression, and that the resulting Neumann / Padé iteration reproduces
`U = (1 − A·G)⁻¹·A`.

### 6.1 Setup

Finite dimensions: `Nα = 4`, `Np = Nq = 3` (so `DIM = 36`). The mock operators
are **deliberately non-symmetric** so that transposition / ordering bugs are
not masked:

- `P` — block-sparse cyclic permutation, `P = P₁₂₃ + P₁₃₂`, `P ≠ P^T` in general.
- `V` — random pair potential, `V ≠ V^T` (asymmetric test).
- `W1` — random spectator-1 3NF, `W1 ≠ W1^T` (asymmetric test).
- `C` — random SWP rotation, `C ≠ C^T` (so `C` and `C^T` are distinguished).
- `G1` — diagonal channel resolvent, `G1[i] = 1/(E − e_i + iε)`.

A single shared seed makes the test reproducible.

### 6.2 Equalities checked (all to `1e-10`)

Let `M_math = C^T·(P·V + W1·(1+P))·C` (dense, assembled directly from the
definitions). Then:

1. **`calculate_CPVC_col` path** — for each column `j`,
   `col_array[j] == M_math[:, j]`. This is the sparse column builder used by the
   dense solver and the precalculated-kernel path.
2. **`calculate_all_CPVC_rows` path** — for each on-shell row `i`,
   `row_array[i, :] == M_math[i, :]`. This is the row builder used by the
   Neumann driving term.
3. **Dense solver path** — `faddeev_dense_solver` with `K = M_math` and
   `G = G1` produces `U_dense = (1 − M_math·G1)⁻¹·M_math`, compared to the
   analytic inverse.
4. **Padé / Neumann path** — the `a_n = A·(G·A)^n` series, Padé-resummed,
   matches `U_dense` to within the Padé tolerance (looser, `1e-6`, because
   Padé is an approximation).
5. **Operator-ordering guard** — the wrong-order kernel
   `M_wrong = C^T·(P·V + (1+P)·W1)·C` is *not* equal to `M_math`, and the
   difference `||M_math − M_wrong||_max > 0` (sanity, not a tolerance).
6. **Transpose guard** — with asymmetric `W1`, the code path
   `calculate_CPVC_col` must compute `W1·C` (not `C·W1` and not `W1^T·C`); a
   deliberate swap inside the test confirms the expected value matches
   `W1·C` and not the alternatives.

This oracle is the **gate** for Phase 3 (independent angular oracle): until
every check in §6.2 passes, no claim about partial-wave matrix elements is
admissible.

---

## 7. Unit contract

The contract fixes the units of every object. No `(2π)³` factor may be inserted
empirically; if a `(2π)³` appears, it must be derivable from the Fourier
convention below.

| Object | Unit | Source |
|--------|------|--------|
| `p, q, p', q'` (Jacobi) | fm⁻¹ | definition |
| `m_π, f_π, Λ_χ, Λ_3NF` | fm⁻¹ | constants.h converted via `÷ħc` |
| `c_1, c_3, c_4` | fm (= GeV⁻¹ × `ħc/1000`) | constructor `m_c1 = c1·ħc/1000` |
| `c_D, c_E` | dimensionless | constructor `m_c_D = c_D` |
| `V^(1)(p',q'; p,q)` (bare 3NF) | fm⁵ | from `(gA/2fπ)²·fm² × 1/fπ² × ...`; closed-form for c_E |
| `p_WP, q_WP` (stored WP boundaries) | MeV | consistent with 2NF `V_WP` |
| `G_array` (channel resolvent) | MeV⁻¹ | from `(E − e_SWP)⁻¹` |
| `W1_WP` (WP-basis 3NF, after bin averaging) | MeV | `W1_raw[fm⁵] × (p_r·q_r·p_c·q_c)·√(dp_r·dq_r·dp_c·dq_c) × 1/ħc⁵`, where the four momenta (MeV) + four `√bin` (MeV^{1/2} each) give MeV⁶, times `1/ħc⁵` (MeV⁻⁵·fm⁻⁵ → but `W1_raw` is fm⁵, so fm⁵×MeV⁶×(1/ħc⁵) = MeV⁶×MeV⁻⁵ = MeV). |

**Fourier convention.** The partial-wave Fourier normalisation
`1/(2π)³ = 1/(8π³)` (`chiral_3nf::fourier_norm_3nf`) is *applied inside the
scalar kernels* `kernel_contact`, `kernel_1pe_contact`, `kernel_2pe_c1c3`. This
mirrors the 2NF convention in `chiral_LO_internal.cpp:59`; the diagnostic
"missing factor of ~(2π)³ = 248" reported in earlier notes is the *symptom* of
the convention, not an independent scaling to be applied. No extra `(2π)³`
factor may be added to fix a magnitude mismatch — a mismatch signals a real bug
in the recoupling or kernel, to be diagnosed against the independent oracle
(Phase 3), not patched.

**Regulator.** Squared-Gaussian per E2002 eq. (3.19):
`f_R(p,q;Λ) = exp(−((4p²+3q²)/(4Λ²))²)`, applied symmetrically to bra and ket.
No alternative regulator form is admissible without amending this contract.

---

## 8. What this contract forbids (carried into the audit report)

The following are **forbidden** as physics-fix tools on this branch (they may
appear only as labelled debug fault-injection, gated to `w1_scale=1.0` in
production):

1. `w1_scale ≠ 1.0` in any production run. The parameter is retained as a
   debug knob; non-unity values must print a strong warning and are
   contract-violating.
2. Global empirical multiplicative factors applied to `W^(1)` to match a
   reference (the historical `~2.4–3.0×` residual is explicitly disallowed).
3. Hand-flipped signs in the kernel to fix a magnitude / sign discrepancy;
   sign conventions must be resolved via the LEC convention (E2002 vs
   Navrátil/Witała), documented once in `kernel_contact`.
4. Post-hoc Hermitian symmetrisation of `W^(1)` (e.g. averaging
   `W1(r,c) + W1(c,r)^T`); the manifestly Hermitian kernel must be derived from
   the 5D angular integral (Phase 3 oracle), not imposed after the fact.
5. Azimuthal-angle averaging dressed up as "true partial-wave projection" in
   comments or docs; the current c_D / c_1 / c_3 path is **rank-0 monopole +
   single-rank-2** and is labelled as such (it is *not* the full G2010 5D PWD).
6. Advertising the model as `chiral_N2LO` (full) while `c_4` is unimplemented;
   the honest name is `chiral_N2LO_c1c3cDcE_approx` until c_4 is independently
   verified (Phase 1).

---

## 9. Acceptance of Phase 0

Phase 0 is accepted when:

- [x] This document exists and every C++ array in §4.2 maps to a math object.
- [ ] `tests/cpp/test_3nf_operator_oracle.cpp` exists and passes all six checks
      of §6.2 on the finite-dimensional random system.
- [ ] The oracle is **independent** of the production recoupling/kernels: it
      builds its own `P, V, W1, G1, C` and compares against a direct dense
      assembly of the math expression, not a re-transcription of the C++.

The remaining unchecked boxes are the Phase 0 work items completed in the
commits that follow this document on `fix/3nf-physics-contract`.
